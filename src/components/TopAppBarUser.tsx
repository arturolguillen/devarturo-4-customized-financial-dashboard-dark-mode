import { User } from "lucide-react";

export default function TopAppBarUser() {
    return (
        <div className="size-8 rounded-full bg-zinc-800 border border-zinc-700 flex items-center justify-center ml-2">
            <User size={20} />
        </div>
    );
}